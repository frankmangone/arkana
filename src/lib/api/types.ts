/**
 * @deprecated This file is kept for backward compatibility.
 * Please import from "./types" instead (e.g., `import { User } from "@/lib/api/types"`).
 * The types are now organized in separate files under `/lib/api/types/*`.
 */
export * from "./types";
